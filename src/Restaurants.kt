//data classes are nothing but classes than hold data bcoz kotlin deals with objects rather than the data inside.
//data class' primary constructor must have atleast 1 parameter
data class Restaurants(var name:String,var rating:Double,var costForOne:Int)